# Legacy Root Notes

This directory keeps historical markdown documents that previously lived in the repository root during the project-memory migration.

These files are archival context, not canonical project memory. New changes should go to `openspec/`, `docs/adr/`, `docs/runbooks/`, `docs/sessions/`, or GitHub issues instead.
