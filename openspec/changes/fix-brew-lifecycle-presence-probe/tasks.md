## 1. Implementation

- [x] 1.1 Add brew presence probing with `present` / `absent` / `unknown` outcomes and installed-version parsing
- [x] 1.2 Wire brew probes into the brew provider adapter and managed-installer compatibility projections
- [x] 1.3 Add regression tests for brew presence probing and installer compatibility

## 2. Validation

- [ ] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`
- [ ] 2.2 Run `bun run openspec:validate`
