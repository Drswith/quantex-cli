## 1. Implementation

- [x] 1.1 Stop removing tracked install state when recorded managed update persistence is cancelled after success.
- [x] 1.2 Add a regression test for the recorded-state update cancellation path.

## 2. Validation

- [x] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`.
