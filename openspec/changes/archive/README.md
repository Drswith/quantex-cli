# Archived Changes

Move completed OpenSpec change folders here after their resulting behavior has been merged into `openspec/specs/`.

The migrated `qtx-*` task history is indexed in [qtx-task-history.md](./qtx-task-history.md). Those entries preserve completed task contracts from the retired `autonomy/` workflow as archived OpenSpec changes.
