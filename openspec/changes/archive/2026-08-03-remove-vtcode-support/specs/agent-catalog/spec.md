## REMOVED Requirements

### Requirement: VTCode MUST be a supported lifecycle agent

**Reason**: VTCode is no longer maintained as a Quantex lifecycle agent.

**Migration**: Existing VTCode installations remain user-managed. Remove them with the installation method originally used if no longer needed; Quantex no longer resolves, installs, inspects, updates, executes, or exports `vtcode`.
