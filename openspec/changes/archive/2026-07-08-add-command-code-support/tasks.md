## 1. Catalog Implementation

- [x] 1.1 Add the Command Code catalog JSON entry with npm install, lookup aliases, version probe, and self-update metadata.
- [x] 1.2 Regenerate checked-in catalog manifests from the catalog directory.

## 2. Tests

- [x] 2.1 Add Command Code registry and metadata coverage to `test/agents.test.ts`.
- [x] 2.2 Verify catalog schema and manifest sync tests include the new entry.

## 3. Validation

- [x] 3.1 Run targeted agent catalog tests.
- [x] 3.2 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, `bun run test`, and `bun run openspec:validate`.

## 4. Closure

- [x] 4.1 Review git/OpenSpec status and report validation, commit, remote, PR, release, and archive-closure state.
