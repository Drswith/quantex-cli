## 1. Implementation

- [x] 1.1 Stop batch install and update loops when the CLI context is cancelled
- [x] 1.2 Guard installed-agent state persistence after cancellation in package-manager lifecycle helpers
- [x] 1.3 Add regression tests for batch install timeout cancellation

## 2. Validation

- [x] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`
- [x] 2.2 Run `bun run openspec:validate`
