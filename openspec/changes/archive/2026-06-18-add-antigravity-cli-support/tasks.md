## 1. Catalog Implementation

- [x] 1.1 Add the Antigravity CLI catalog JSON entry with aliases, binary, installer, version probe, and self-update metadata.
- [x] 1.2 Regenerate checked-in catalog manifests so runtime exports include Antigravity CLI.
- [x] 1.3 Export and test the Antigravity CLI entry from the agent module.

## 2. Validation

- [x] 2.1 Run focused agent catalog tests.
- [x] 2.2 Run required validation: `bun run lint`, `bun run format:check`, `bun run typecheck`, `bun run test`, and `bun run openspec:validate`.
