# project-memory Delta

## REMOVED Requirements

### Requirement: Root markdown allowlist SHALL track canonical README entry points (duplicate)

**Reason**: This requirement was stated twice in the same specification; the first occurrence remains the contract.

**Migration**: Follow the first `Root markdown allowlist SHALL track canonical README entry points` requirement in `openspec/specs/project-memory/spec.md`.
