## 1. OpenSpec And Product Copy

- [x] 1.1 Update the product-readme and project-memory spec deltas for English-first README entry points.
- [x] 1.2 Change the package description and README language-entry copy to lead with English while preserving Simplified Chinese access.

## 2. Repo Pointers And Validation

- [x] 2.1 Update repo-native checks and contributor pointers to recognize the canonical README filenames.
- [x] 2.2 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, `bun run openspec:validate`, and `bun run memory:check`.
