## 1. Implementation

- [x] 1.1 Validate `installedAgents` during `readState()` and throw `StateFileError` on invalid records.
- [x] 1.2 Add regression tests for unknown `installType` and non-object `installedAgents`.

## 2. Validation

- [x] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`.
