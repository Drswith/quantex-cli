## 1. Implementation

- [x] 1.1 Restrict npm path inference in `inferManagedInstallTypeFromBinaryPath()`
- [x] 1.2 Add regression tests for project-local and global npm binary paths

## 2. Validation

- [x] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`
- [x] 2.2 Run `bun run openspec:validate`
