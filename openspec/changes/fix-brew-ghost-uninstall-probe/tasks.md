## 1. Implementation

- [x] 1.1 Add brew presence probing with `present` / `absent` / `unknown` outcomes
- [x] 1.2 Wire brew `probePackagePresence` into managed ghost recovery
- [x] 1.3 Add regression tests for brew ghost recovery paths

## 2. Validation

- [x] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`
