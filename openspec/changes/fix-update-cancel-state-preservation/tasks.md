## 1. Implementation

- [ ] 1.1 Stop removing installed-agent state when `updateAgent()` observes cancellation after persistence
- [ ] 1.2 Add regression tests for managed update cancellation during state persistence

## 2. Validation

- [ ] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`
