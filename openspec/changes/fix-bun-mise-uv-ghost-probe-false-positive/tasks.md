## 1. Implementation

- [x] 1.1 Add bun scoped presence probing with `present` / `absent` / `unknown` outcomes
- [x] 1.2 Add mise and uv presence probing with fail-closed inconclusive handling
- [x] 1.3 Wire installer `probePackagePresence` hooks into ghost recovery
- [x] 1.4 Add regression tests for inconclusive probe and confirmed absence paths

## 2. Validation

- [x] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`
