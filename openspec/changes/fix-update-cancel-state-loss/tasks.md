## 1. Update cancellation guard

- [ ] 1.1 Stop deleting recorded install state when a managed update is cancelled after state re-persistence
- [ ] 1.2 Add package-manager regression test for cancelled managed update with recorded state

## 2. Validation

- [ ] 2.1 Run `bun run lint`, `bun run format:check`, `bun run typecheck`, and `bun run test`
- [ ] 2.2 Run `bun run openspec:validate`
